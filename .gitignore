# Local Jekyll build output
_site/
.jekyll-cache/
.jekyll-metadata

# Gemfile.lock is committed: the GitHub Actions build resolves against it,
# so the deployed site uses the same gem versions as a local build.
